import Foundation

/// Namespace for OverprintKit, the engine shared by the CLI, the macOS app, and any agent.
public enum Overprint {
    /// The OverprintKit version, surfaced by front-ends (CLI `--version`, the app about box).
    public static let version = "0.1.0"
}
